import React, { useState, useEffect, useRef, isValidElement } from 'react';
import { View, Text } from 'react-native';

export default () => {
    return (
        //Formulário Empilhadeiras
        <View>
            <Text>Empilhadeiras</Text>
        </View>
    );
}